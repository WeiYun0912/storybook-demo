import Box2 from "./Box2";

export default {
  title: "Component/Box2",
  component: Box2,
};

// const Template = (args) => <Box2 {...args} />;

// export const largeBox = Template.bind({});

// largeBox.args = {
//   label: "Large Box",
// };

// export const mediumBox = Template.bind({});

// mediumBox.args = {
//   label: "Medium Box",
// };
